<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="main">

	<select id="selectAddress" resultType="city">
		SELECT CITY_SEQ            AS citySeq,
		       CITY_FIRST_SECTION  AS cityFirstSection,
		       CITY_SECOND_SECTION AS citySecondSection,
		       INSERT_DATE         AS insertDate,
		       INSERT_USER         AS insertUser,
		       CITY_CODE           AS cityCode
		FROM CITYTBL
		ORDER BY CITY_FIRST_SECTION DESC,
		         CITY_SECOND_SECTION
	</select>

</mapper>